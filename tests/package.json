{
  "name": "@caterkingapp/tests",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:chaos": "playwright test --grep \"chaos\""
  },
  "devDependencies": {
    "vitest": "^1.6.1",
    "@playwright/test": "^1.48.0"
  }
}
